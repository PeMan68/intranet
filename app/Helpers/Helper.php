<?php
 
if (!function_exists('update_table_responsibilites')) {
    /**
     * Updates the table responsibilities from actual tasks in table tasks.
     *
	 * When a new task is added to tasks, or a task is dropped from tasks,
	 * the table priorities needs to be updated, so all users have the 
	 * correct pririty for each available task.
	 *
     * @return void
     *
     * */
    function update_table_responsibilites()
    {
		//
    }
}